# uni_lev

#python set up

in the project root directory create a vertual enviroment with:
python -m venv unilev_venv

activate it with:
unilev_enve\Scripts\activete

install dependecies with:
python -m pip install -r backend\requiments.txt
